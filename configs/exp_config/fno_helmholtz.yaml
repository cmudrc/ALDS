# must have configs
n_clusters: 4
save_mode: 'save_png'

# encoder configs
n_components: 4
domain_size: 0.1963495

# classifier configs

# model configs
in_channels: 1
out_channels: 1
modes: 8
width: 8

# data configs
root: 'data/helmholtz'
partition: True
sub_size: 40
num_samples: 3000
freq_bandwith: (1, 10000)

# pred configs
idxs: [1, 200, 400, 600, 800]
timesteps: 1